class Question {
    constructor({name, sequenceNumber, active}) {
        this.active = active;
        this.name = name;
        this.sequenceNumber = sequenceNumber;
    }
}

export default Question;